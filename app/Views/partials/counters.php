<?php
/**
 * СЧЁТЧИКИ ПОСЕЩАЕМОСТИ
 * =====================
 *
 * Сюда вставляется код Яндекс Метрики и любых других счётчиков.
 *
 * КАК ВСТАВИТЬ МЕТРИКУ
 * 1. Метрика → ваш счётчик → «Настройка» → «Код счётчика» → «Скопировать код».
 * 2. Вставьте скопированное ниже, между строками «НАЧАЛО» и «КОНЕЦ».
 * 3. Загрузите этот файл на сайт, в папку app/Views/partials.
 *
 * Ничего больше менять не нужно: файл подключается в конце каждой
 * страницы сайта сам.
 *
 * Код вставляется целиком, как его дала Метрика, — вместе с <script>
 * и <noscript>. Переписывать его руками не надо: Яндекс время от времени
 * меняет код счётчика, и рабочим остаётся именно тот, что скопирован
 * из личного кабинета.
 *
 * В админку сайта счётчик не подключается намеренно: ваши собственные
 * заходы в неё не должны попадать в статистику посещаемости.
 *
 * Пока здесь пусто, на страницах не выводится ничего.
 */
?>
<?php // ------------------------- НАЧАЛО кода счётчиков ------------------------- ?>



<?php // -------------------------- КОНЕЦ кода счётчиков -------------------------- ?>
