User-agent: *
Allow: /
Disallow: /zayavka
Disallow: /admin
Disallow: /api/

# Отбор статей по темам и поиск по сайту.
#
# Это не страницы, а выборки из списка: содержимое то же, меняется только
# состав. Закрыть их от индексации мало — чтобы увидеть запрет, робот
# должен сначала скачать страницу. А адресов у отбора столько, сколько
# сочетаний тем, и робот уходит обходить их вместо статей. Поэтому обход
# сюда запрещён целиком.
#
# Звёздочка в начале нужна: параметр бывает и первым, и не первым —
# «?tegi=» и «?q=crm&tegi=».
Disallow: /*tegi=
Disallow: /*q=

# Страницы списка (?stranica=2 и дальше) намеренно не закрыты: это путь
# к статьям, которые не поместились на первую.

# Сборщики, которые не приводят посетителей.
#
# За 21 сентября Applebot, meta-externalagent и Amazonbot дали три
# четверти всей нагрузки на сайт — больше шестисот тысяч запросов
# за сутки, и ни одного человека. Они собирают тексты для обучения
# своих моделей и для чужих справочников.
#
# Это просьба, а не запрет: робот может её не послушать. Настоящий
# отказ стоит в .htaccess, там запрос не доходит до сайта вовсе.
# Здесь — для тех, кто правила соблюдает.
#
# Закрыты только те, кто собирает тексты для обучения модели: в ответах
# такой модели ссылки на источник нет. Те, кто отвечает человеку
# со ссылкой на сайт, не закрыты намеренно — PerplexityBot,
# OAI-SearchBot, ChatGPT-User, Claude-User. И тем более не закрыты
# YandexBot с Googlebot: именно они собирают ИИ-ответы Яндекса
# и Google, где русскоязычный посетитель и может вас найти.
User-agent: Applebot
User-agent: meta-externalagent
User-agent: Amazonbot
User-agent: Bytespider
User-agent: GPTBot
User-agent: ClaudeBot
User-agent: anthropic-ai
User-agent: CCBot
User-agent: AhrefsBot
User-agent: SemrushBot
User-agent: MJ12bot
User-agent: DotBot
User-agent: BLEXBot
User-agent: DataForSeoBot
User-agent: PetalBot
Disallow: /

# Адрес карты сайта. Заменить домен при выкладке на боевой сервер.
Sitemap: https://ivanborkovsky.ru/sitemap.xml
