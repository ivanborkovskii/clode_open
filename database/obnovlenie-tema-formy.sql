-- Тема статьи в форме заявки.
--
-- Выполняется один раз на уже работающей базе: phpMyAdmin → выбрать базу →
-- вкладка «SQL» → вставить это и нажать «Вперёд».
--
-- Поле необязательное. Пока оно пустое, тема подставляется автоматически
-- из заголовка статьи, а если и это не получилось — форма показывает
-- обычный текст, как на остальных страницах сайта.

ALTER TABLE articles
    ADD COLUMN form_topic VARCHAR(200) NOT NULL DEFAULT '' AFTER meta_description;
